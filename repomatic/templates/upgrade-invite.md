---
args: [previous, adopted, command]
footer: false
---

## 🧭 Upgrade review

This pull request moves `repomatic` from `\$previous` to `\$adopted`. To review what the new release lets this repository adopt, reuse or drop, run the [`/repomatic-upgrade`](https://repomatic.net/agent-skills) skill in a fresh session:

```shell
$command
```
