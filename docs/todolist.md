# {octicon}`checklist` Todo list

Every pending item this project tracks, collected from the `{todo}` admonitions left in the documentation pages and in the docstrings `autodoc` renders. Each entry links back to where it is written, which is next to the code or the prose it acts on.

Most wait on an upstream release: the ticket named in an item is what closes it.

```{todolist}
```
